import { encodeSha256 } from "@/lib/crypto";
import { prismaClient } from "@/lib/prisma";

describe("Signin route", () => {
  test("when access POST route '/api/v1/signgin' contains in body the username and password correclty", () => {});
});
